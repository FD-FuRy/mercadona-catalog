spring:
  datasource:
    url: jdbc:postgresql://db_adress/db_name
    username: nom_utilisateur
    password: mdp_utilisateur
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
springdoc:
  api-docs:
    path: /api-mercadona_docs
  swagger-ui:
    path: /swagger.html
